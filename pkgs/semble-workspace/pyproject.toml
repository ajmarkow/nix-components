[project]
name = "semble-tool"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["semble[mcp]>=0.3.0"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.uv]
package = false
