[project]
name = "mcpm-tool"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["mcpm>=1.13.1"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.uv]
package = false
